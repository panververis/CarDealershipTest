(function () {
    'use strict';

    angular
        .module('app')
        .controller('main', main);

    main.$inject = ['$scope']; 

    function main($scope) {
        $scope.Search = function() {
            
        }
        //activate();

        //function activate() { }
    }
})();
